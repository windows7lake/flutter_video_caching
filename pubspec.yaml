name: flutter_video_caching
description: "A video cache project, support mp4, m3u8."
version: 0.1.0
homepage: https://github.com/windows7lake/flutter_video_caching

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ^3.0.6
  flutter_hls_parser: ^2.0.1
  http: ^1.3.0
  path_provider: ^2.1.5
  synchronized: ^3.1.0+1
  logger: ^2.5.0

flutter: