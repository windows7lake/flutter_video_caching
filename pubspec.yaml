name: flutter_video_cache
description: "A video cache project, support mp4, m3u8."
version: 0.0.1
homepage:

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ^3.0.6
  drift: ^2.25.1
  drift_flutter: ^0.2.4
  flutter_hls_parser: ^2.0.1
  http: ^1.3.0
  path_provider: ^2.1.5
  pool: ^1.5.1
  synchronized: ^3.1.0+1
  log_wrapper: ^1.0.3
  logger: ^2.5.0
  uuid: 4.5.1

dev_dependencies:
  drift_dev: ^2.25.2
  build_runner: ^2.4.15

flutter:
